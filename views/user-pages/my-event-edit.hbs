<h1>Nuevo evento</h1>
<form action="/usuario/mi-evento/editar/{{event.id}}" method="POST">
    <div>
        <label for="title">Título del evento</label>
        <input type="title" id="title" name="title" value="{{event.title}}">
    </div>

    <div>
        <label for="date">Fecha del evento</label>
        <input type="datetime-local" id="date" name="date" value="{{trueDate}}">
    </div>

    <div>
        <label for="city">Ciudad</label>
        <input type="String" id="city" name="city" value="{{event.city}}">
    </div>

    <div>
        <label for="place">¿En dónde es tu evento?</label>
        <input type="text" id="place" name="place" value="{{event.place}}">
    </div>

    <div>
        <label for="latitude">Longitud</label>
        <input type="number" id="latitude" name="latitude" step="any" value="{{event.location.coordinates.[0]}}">
    </div>

    <div>
        <label for="longitude">Latitud</label>
        <input type="number" id="longitude" name="longitude" step="any" value="{{event.location.coordinates.[1]}}">
        <small>Para ubicar el evento, entra a Google Maps y con el click derecho te aparecerán las coordenadas de Latitud y Longitud.</small>
    </div>

    <div>
        <label for="img">¿El evento tiene una foto o imagen promocional? Comparte aquí la URL</label>
        <input type="text" id="img" name="img" value="{{event.img}}">
    </div>

    <button type="submit">Create event</button>
</form>
<a href="/usuario/perfil">Regresar a perfil</a>

{{#if errorMsg}}
<div class="error-message">{{ errorMsg }}</div>
{{/if}}