<div class="container">

    <div class="month-searchbar">
        <form class="form-inline" action="/eventos/{{city}}" method="POST">
            <div class="form-group mx-sm-3 mb-2 text-center">
                <label for="month" class="sr-only">Month</label>
                <select name="month" id="month" class="form-control">
                    <option value="">Elige un mes</option>
                    <option value="01">Enero</option>
                    <option value="02">Febrero</option>
                    <option value="03">Marzo</option>
                    <option value="04">Abril</option>
                    <option value="05">Mayo</option>
                    <option value="06">Junio</option>
                    <option value="07">Julio</option>
                    <option value="08">Agosto</option>
                    <option value="09">Septiembre</option>
                    <option value="10">Octubre</option>
                    <option value="11">Noviembre</option>
                    <option value="12">Diciembre</option>
                </select>
            </div>
            <button type="submit" class="btn btn-dark mb-2">Filtrar</button>
        </form>
    </div>

    {{#if yesEvents}}
    {{#if localEvents.[0]}}
    <h2>Eventos de nuestros artistas locales</h2>

    <div class="row justify-content-center">
        {{#each localEvents}}
        <div class="col-xs-12 col-md-4 event-card local-card align-self-center">
            <div class="row">
                <div class="col-8 align-self-center">
                    <h5>{{title}}</h5>
                    <p class="card-artist-name">{{artist.artisticName}}</p>
                    <p><img class="marker-logo" src="/images/marker-logo.png" alt="Marker logo"> {{city}}</p>
                    <a href="/eventos/locales-detalles/{{id}}">Detalles evento</a>
                </div>
                {{#if artist.img}}
                <div class="col-4 align-self-center">
                    <img class="img-card" src="{{artist.img}}" alt="Artist img">
                </div>
                {{/if}}
            </div>
        </div>
        {{/each}}
    </div>
    {{/if}}
    

    
    {{#if eventsObj}}
    <h2>Otros eventos</h2>

    <div class="row justify-content-center tm-card">
        {{#each eventsObj}}
        <div class="col-xs-12 col-md-4 event-card">
            <h5>{{name}}</h5>
            <p>{{_embedded.attractions.[0].name}}</p>
            <p><img class="marker-logo" src="/images/marker-logo.png" alt="Marker logo">
                {{_embedded.venues.[0].city.name}}</p>
            <a href="/eventos/detalles/{{id}}">Detalles evento</a>
        </div>
        {{/each}}
    </div>
    {{/if}}
    

    {{else}}
    <div class="row justify-content-center text-center div-wrapper d-flex align-items-center">
        <div class="col no-events">
            <img src="/images/search.png" alt="search icon">
            <h3>No hay eventos en el mes seleccionado</h3>
        </div>
    </div>
    {{/if}}
    
</div>







<button onclick="topFunction()" id="myBtn" title="Go to top">Subir</button>


<script src="/javascripts/script.js"></script>